# Handoff notes — recent session context (не в CLAUDE.md)

Этот файл — сводка архитектурных решений и текущего состояния, накопленных
за одну длинную рабочую сессию (Claude Code). Не входит в постоянную
документацию проекта (`CLAUDE.md` — читать в первую очередь, там
architecture conventions). Цель этого файла — чтобы другой агент/разработчик
не переизобретал уже принятые решения и не наступал на грабли, о которые
здесь уже наступили.

## Хронология PR (все смёржены в `main`, кроме последнего)

| PR | Что сделано |
|---|---|
| #117 | Настройки чата: новая вкладка «Фильтры» (12 типов контента вместо 7+5), «Новые пользователи» (капча+приветствие), Automod убрал обёртку-заголовок «Правила чата», `/rules` и `rulesText` удалены из бота полностью |
| #118 | Клик «Команды» в сайдбаре больше не открывает новую вкладку браузера |
| #119 | Ширина контейнера `/commands` выровнена под остальные страницы |
| #120 | Список «Группы» скрывает чаты, откуда бот вышел/отключён (было: показывал вообще всю историю) |
| #121 | Реальные аватарки чатов из Telegram (`Chat.photoFileId`+`avatarSyncedAt`, зеркалит существующий `TelegramUser`-паттерн), все аватарки в панели — круглые |
| #122 | **Chat-scoped self-service вход через Telegram**: новый `AdminUser.scope` (GLOBAL/CHAT), таблица `ChatAdminAccess`, вкладка «Команда» на уровне чата (реальные Telegram-админы read-only + добавление по @username только уже известных боту людей) |
| #123 | Фикс: chat-scoped владелец реально мог редактировать свой чат (`resolveEffectiveChatRole`), закрыта дыра в 5 роутах без проверки доступа к чату, «Обзор»/«Журнал» включены обратно с данными **только по своим чатам** |
| *(не смёржен)* | `feat/simplify-moderation-notifications` — см. ниже |

## Ключевые архитектурные решения, которые нужно уважать

1. **Multi-tenancy добавлена поверх старой single-tenant модели, не заменяет её.** `AdminUser.role` (глобальная роль) — для email/password операторов, видят всё. `AdminUser.scope: "CHAT"` — самостоятельно зарегистрированные через Telegram владельцы чатов, видят только свои чаты через `ChatAdminAccess`. Для CHAT-scoped `role` всегда инертный `"VIEWER"`, реальные права — только через `resolveEffectiveChatRole(admin, chatId)` (`src/server/auth/guards.ts`).
2. **`ChatAdminAccess` (веб-панель) и `ChatRole`/`ChatMember` (права на команды бота внутри Telegram) — две независимые системы.** Не путать и не пытаться объединить — разный смысл, разные потребители.
3. **`requireChatAccess(admin, chatId)` возвращает честный 404, не 403** — chat-scoped админ не должен уметь отличить «чат не мой» от «чата не существует». Этот паттерн используется везде, где раньше проверялась только глобальная роль.
4. **Каждый новый `AuditLog`-action нужно вручную добавлять в вайтлист `journal-service.ts`** (`JOURNAL_ACTIONS`/`SETTINGS_ACTIONS`) — иначе строка есть в БД, но никогда не покажется в «Журнал». Забывали дважды за сессию, ловили только на ревью.
5. **Миграции пишутся руками** (`prisma migrate dev` недоступен без живого Postgres) — смотреть 2-3 последних файла в `prisma/migrations/` для стиля (без down-migration, явные имена constraint'ов).
6. **Сеть в песочнице (эта среда) нестабильна для Prisma CLI** (`prisma migrate deploy` регулярно падает с `Connection terminated unexpectedly`/`P1002`), но **обычный `pg`-драйвер работает надёжно**. Рабочий обход: применять миграцию вручную через короткий Node-скрипт на `pg` (открыть соединение, выполнить SQL, записать bookkeeping-строку в `_prisma_migrations` с sha256-checksum файла) — сработало во всех случаях. CI на чистом Postgres всегда проходил зелёным даже когда локальные тесты в песочнице падали с той же сетевой ошибкой — **CI считать авторитетным**, не гнаться за локальным зелёным прогоном в этой среде.
7. **Публичный `/commands`** (справка по командам бота) намеренно остаётся публичной страницей без логина, но теперь показывает сайдбар залогиненным админам (`getCurrentAdmin()` без редиректа, единственное такое место в кодовой базе).

## Полностью отложенное, не начатое

- **Показ CHAT-scoped аккаунтов в «Система → Аккаунты»** для аудита/деактивации владельцем — сейчас список видит только GLOBAL-аккаунты.
- **`promoteChatMember`-опция** («сделать реальным Telegram-админом при добавлении в Команду», как у Combot `promote_in_telegram`) — не запрошено, реальный побочный эффект в Telegram, осознанно не делали.

## В процессе (ветка `feat/simplify-moderation-notifications`, локально, не запушена)

Полный аудит уведомлений модерации (артефакт был опубликован пользователю) выявил кучу несогласованностей. По итогам согласовано и реализуется:
- Убрать приватное DM-уведомление при наказании (warn/mute/ban), оставить только ephemeral-сообщение в чате.
- **Важное следствие**: `/appeal` раньше матчился на наказание через `metadata.appealDmMessageId === replyToMessageId` — ID того самого DM. Без DM это физически ломается (ephemeral-сообщения к тому же всегда `message_id=0`). `/appeal` переписан без Reply — сам находит последнее наказание, с числовой дизамбигуацией как у `/unmute`, если наказаний в нескольких чатах.
- Причина у warn/mute/ban/kick стала необязательной.
- Апелляции — тумблер + настройки уведомлений на уровне чата (новая таблица `ChatAppealSettings`), тексты вынесены в редактируемый набор (новая `GlobalAppealSettings`, поверх той же UI-страницы «Система → Уведомления»).
- «Снять предупреждение» добавлено в Web Admin (раньше — только команда `/unwarn` в чате).

Полный план — если нужен точный контекст решений — лежал в
`C:\Users\Алексей\.claude\plans\agile-hatching-finch.md` (файл плана Claude
Code, может быть перезаписан следующей задачей — если важно, скопируйте
раньше).
